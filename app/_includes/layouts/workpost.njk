<!DOCTYPE html>
<html lang="en" class="no-js">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
      {{title}}
    </title>
    {% if description %}
      <meta name="description" content="{{description}}"/>
    {% endif %}
    {% block head %}{% endblock %}
    <link rel="stylesheet" type="text/css" href="/static/css/main.css"/>
  </head>
  <body class="page--{{path}}">
    {% include "partials/navigation.njk" %}

    <main class="content">
        <div class="contained-width">
            <article class="page--workpost__content">
                <div class="page--workpost__body">
                    <header>
                        <ul class="page--workpost__media">
                          {% for medium in medium %}
                            <li>
                              <a href="/work/{{medium | slugify}}">{{medium | title}}</a>{% if not loop.last %}<span aria-hidden="true">/</span>{% endif %}
                            </li>
                          {% endfor %}
                        </ul>
                        <h1 class="page--workpost__title">{{title or eleventyComputed.title}}<h1>
                    </header>
                    <div class="prose prose-stone">
                        {{ content | safe }}
                    </div>
                    <ul class="taglist">
                      {% for tag in tags %}
                        {% if tag != 'work' %}
                          <li>
                            <a href="/tags/{{tag | slugify}}" class="taglist__tag">{{tag | title}}</a>
                          </li>
                        {% endif %}
                      {% endfor %}
                    </ul>
                </div>
                <div class="page--workpost__image-container glide">
                    <div class="glide__track" data-glide-el="track">
                      <ul class="page--workpost__images glide__slides no-glide">
                          {% for image in images %}
                              <li class="page--workpost__image glide__slide">
                                <img src="{{image.src}}" alt="{{image.alt}}">
                              </li>
                          {% endfor %}
                      </ul>
                      <div class="glide__arrows" data-glide-el="controls">
                        <button class="glide__arrow glide__arrow--left" data-glide-dir="<" aria-label="Previous">
                          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" aria-hidden="true">
                            <path fill="#FFF" d="M7.293 4.707 14.586 12l-7.293 7.293 1.414 1.414L17.414 12 8.707 3.293 7.293 4.707z"/>
                          </svg>
                        </button>
                        <button class="glide__arrow glide__arrow--right" data-glide-dir=">" aria-label="Next">
                          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" aria-hidden="true">
                            <path fill="#FFF" d="M7.293 4.707 14.586 12l-7.293 7.293 1.414 1.414L17.414 12 8.707 3.293 7.293 4.707z"/>
                          </svg>
                        </button>
                      </div>
                    </div>
                </div>
            </article>  
        </div>
    </main>

    {% include "partials/footer.njk" %}
  </body>
  <script src="/static/js/main.js"></script>
</html>